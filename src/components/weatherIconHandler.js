export default function weatherIconHandler (reqest) {
   switch (reqest) {
      case ("Cloudy" || "partly-cloudy-day" || ):
         return "../img/cloudy.png"
         
   
      default:
         break;
   }

}